import 'package:flutter/material.dart';

class ConfirmEmailView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        //TODO update human message
        child: Text('Confirm email to continue'),
      ),
    );
  }
}
