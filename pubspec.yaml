name: rewardstender
description: A new Flutter application.
version: 1.0.0+1

environment:
  sdk: ">=2.8.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  flutter_barcode_scanner: ^2.0.0
  firebase_auth: 1.1.2
  firebase_core:
  firebase_database:
  cached_network_image:
  intl: any
  provider:
  flip_card: ^0.5.0
  flutter_calendar_carousel: any
  camera: ^0.8.1
  flutter_barcode_sdk: ^0.0.9
  image: ^3.0.2

  rewards_network_shared:
    git:
      url: https://github.com/Josecodesalot/rewards_network_shared.git
      ref: master

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

